{
  "name": "url-shortener",
  "version": "1.0.0",
  "description": "A simple URL shortener built on Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "kv:create": "wrangler kv:namespace create URLS"
  },
  "keywords": ["cloudflare", "workers", "url-shortener"],
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
